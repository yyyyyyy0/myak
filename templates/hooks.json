{
  "Stop": [
    {
      "matcher": "*",
      "hooks": [
        {
          "type": "command",
          "command": "myak-index",
          "async": true
        }
      ]
    }
  ],
  "UserPromptSubmit": [
    {
      "matcher": "*",
      "hooks": [
        {
          "type": "command",
          "command": "query=$(cat | python3 -c \"import sys,json; d=json.load(sys.stdin); print(d.get('message','')[:200])\" 2>/dev/null); [ ${#query} -ge 3 ] && myak-query --hook \"$query\" 2>/dev/null || true"
        }
      ]
    }
  ]
}
